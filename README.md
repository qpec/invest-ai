# bot/seed

Box-bootstrap data that deliberately lives outside the code history:
public screening metadata only (the same names/sectors the public desk site
already lists). Fetched by the first-boot deploy into the state tree.
