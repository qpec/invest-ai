# toolchain / build
.venv/
__pycache__/
*.py[cod]
dist/
build/
*.egg-info/
.pytest_cache/

# state — never lives in the code repo (tech-arch §1.1); guards desk runs
# where AGENTCY_STATE_DIR points inside the checkout
*.db
*.db-wal
*.db-shm
.state/
spool/
locks/
